using UnityEngine;
using System.Collections;
using System.Threading;
public class TerrainThreadManager : MonoBehaviour {
	private TerrainMod terrainMod;
	// Use this for initialization
	void Start () {
		//terrainMod = gameObject.GetComponent<TerrainMod>();
		//Thread t = new Thread(terrainMod.StartWork());
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
